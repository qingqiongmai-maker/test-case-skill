---
name: camera-app-testcases
description: 面向监控摄像机 App 功能迭代的需求测试分析技能。用于根据产品需求文档，按资深 App 测试人员工作流完成需求理解、测试点提取、需求批注、测试环境/物资清单、测试用例设计。输入为需求文档，输出固定为：需求名称-测试点.md、需求名称+需求批注.docx、需求名称+测试清单.md、需求名称+测试用例.xlsx。
---

# 监控摄像机 App 需求测试分析

本技能用于监控摄像机 App 每次功能迭代的测试分析工作。默认使用中文输出。

你要扮演资深 App 测试人员，围绕用户提供的需求文档完成四类交付物：

1. `需求名称-测试点.md`
2. `需求名称+需求批注.docx`
3. `需求名称+测试清单.md`
4. `需求名称+测试用例.xlsx`

所有交付物必须保存到输出目录下的 `需求名称+测试` 文件夹中。

## 工作流总览

按照测试人员的正常工作顺序执行：

1. 读取需求文档。
2. 理解需求背景、功能范围、影响模块、端侧范围、设备/服务依赖。
3. 对需求文档中“详细需求”下的功能点进行初步需求分析和测试点提取，只以 XMind 可完整导入的 Markdown 标题大纲形式输出测试点文件。
4. 站在测试角度和产品角度，对原 Word 需求文档副本插入批注，协助产品完善需求描述。
5. 根据需求文档和测试点文件，整理测试环境、测试工具、测试物资、账号、设备、外部依赖等，输出测试清单。
6. 根据需求文档和测试点文件，输出覆盖测试点的 Excel 测试用例。
7. 交付前执行质量自检：检查测试点是否重复/空泛、用例是否可执行、测试点与用例是否可追溯、四份文件命名和格式是否正确。
8. 最终回复列出输出文件夹和四份文件路径，并简要说明覆盖范围、批注数量、风险和待确认项。

## 输入与输出

### 输入 1：需求文档

用户提供 Word、PDF、Markdown、TXT、Excel 或粘贴的需求内容时，优先以原始需求文档为准。若需求文档包含原型图、截图、表格、流程图，要同时读取正文和可见图片信息；图片内容无法识别时，在输出中标记为“图片内容待确认”。

### 输出 1：`需求名称-测试点.md`

输入依据：需求文档。

作用：对需求文档中“详细需求”下的功能点进行初步需求分析，提取测试点并以 XMind 可完整导入的 Markdown 标题大纲形式输出。该文件用于快速呈现“这个需求要从哪些方向测”，不要求每个测试点都展开成测试项，不输出表格、不输出测试步骤、不输出清单。

### 输出 2：`需求名称+需求批注.docx`

输入依据：需求文档、测试点分析过程。

作用：基于原 Word 需求文档另存副本，在不修改原文档正文的前提下，对原需求文案中模糊、不明确、考虑不全、需要补充完善或可优化的位置插入 Word 批注。

### 输出 3：`需求名称+测试清单.md`

输入依据：需求文档、测试点文件。

作用：整理执行本需求测试所需的测试环境、测试工具、测试物资、设备、账号、网络、后台/云服务、外部依赖、冒烟范围、回归范围、发布前确认项。

### 输出 4：`需求名称+测试用例.xlsx`

输入依据：需求文档、测试点文件。

作用：对需求和测试点进行覆盖，输出可执行的详细测试用例。测试用例通过需求功能名称和关联测试点名称追溯到测试点文件。

## 输出目录与命名

- 从需求文档标题、文件名或版本信息提取需求名称。
- 清理 Windows 非法字符：`/ \ : * ? " < > |`。
- 若用户指定输出目录，在该目录下创建：`需求名称+测试`。
- 若用户未指定输出目录，在需求文档所在目录下创建：`需求名称+测试`。
- 不要删除同名文件夹中的无关内容；只覆盖本次要生成的同名交付文件。

## 1. 需求分析步骤

读取需求文档后，先完成以下分析，不要直接写用例：

1. 提取基础信息。
   - 需求名称、版本号、变更记录、涉及端：Android、iOS、H5、服务端、设备端、平台端。
   - 涉及模块：添加设备、扫码、登录注册、设备列表、实时预览、回放、告警、云台、消息推送、设置、分享、云存储、SD 卡、固件升级等。

2. 拆解需求点。
   - 新增功能是什么。
   - 修改了哪些旧流程。
   - 删除或恢复了哪些旧逻辑。
   - 哪些功能只在特定地区、语言、账号、设备型号、固件版本、平台类型下生效。
   - 每个需求点必须拆出“入口、触发动作、系统判断、页面/接口/设备结果、失败兜底、回归影响”。
   - 不要只照抄需求标题作为测试点；必须先判断该需求真实改变了哪个用户路径或系统链路。

3. 判断影响范围。
   - 直接测试范围。
   - 需要回归的旧功能。
   - 可能受影响的公共能力，例如登录态、设备连接、推送、权限、云端接口、H5 跳转。
   - 明确影响对象：App 页面、系统权限、云端接口、设备端状态、H5 页面、应用商店、后台配置、本地缓存、消息推送、日志/埋点。
   - 标记高风险链路：跨 App、跨端跳转、设备状态同步、云端状态同步、弱网恢复、账号权限和地区语言规则。

4. 识别外部依赖。
   - 摄像机型号、固件版本、4G/SIM 卡、SD 卡、云服务、后台开关、H5 页面、应用商店链接、服务端接口、地区/语言配置。

5. 标记需求问题。
   - 模糊描述。
   - 功能规则不完整。
   - 异常流程缺失。
   - 边界条件缺失。
   - 文案和原型不一致。
   - 兼容性、权限、设备状态、网络状态考虑不足。
   - 错误码、状态优先级、按钮动作、刷新时机、验收标准缺失。
   - 需求无法支撑测试设计时，不要自行脑补规则；在测试点中保留“待确认风险”，并在 Word 批注中要求补充。

6. 建立需求功能到用例的关联关系。
   - 测试点文件只做需求功能分析，不加编号。
   - 后续测试清单和测试用例通过“需求功能名称”和“测试点名称”关联，不要求测试点编号。

## 2. 功能测试反向设计模型

从“最终要证明什么正确、什么风险不能漏”反向设计测试点和用例。不要堆方法论，也不要为每个功能机械套层级。

### 2.1 反向设计步骤

1. 明确可验收结果：页面展示、接口返回、本地缓存、云端状态、设备状态、H5/应用商店跳转、日志/埋点中哪些结果必须正确。
2. 反推失败条件：哪些输入、状态、权限、网络、设备、账号、地区/语言、服务端异常会让结果出错。
3. 选择设计方法：流程用场景法，输入用等价类/边界值，多条件规则用判定表，状态变化用状态迁移，多维组合用 Pairwise/正交，高危经验用错误推测。
4. 形成测试点：每个测试点必须写清“触发条件 + 风险/验证目标 + 可观察结果”。
5. 生成用例：每条用例只验证一个主要风险；同一风险的机型/网络差异合并为组合用例。

### 2.2 功能测试必问清单

每个需求功能优先追问以下问题，只保留与当前需求真实相关的问题：

- 主流程：用户从哪个入口进入，执行什么动作，最终应看到什么结果。
- 分支规则：不同账号、设备状态、平台、地区/语言、权限、服务端返回会进入哪个分支。
- 输入输出：输入值、二维码、密码、H5 参数、接口字段的有效/无效/边界值是什么，输出是否同步到界面、云端、设备端和日志。
- 状态变化：未开始、处理中、成功、失败、超时、中断、重试、返回后状态如何流转。
- 异常恢复：断网、弱网、杀进程、前后台切换、权限拒绝、服务端失败后是否可恢复。
- 兼容回归：该需求依赖的系统 API、应用商店、H5、相机、通知、旧设备/旧固件/旧流程是否需要抽样覆盖。
- 状态 + 事件：设备在线/离线/关机/升级中、App 前台/后台/重启、网络正常/弱网/断网、服务端成功/失败/超时，与用户点击、返回、重试、刷新等事件组合后会发生什么。
- 跨端链路：App 操作后，云端接口、设备端状态、H5/应用商店、系统权限、本地缓存和 App 返回状态是否一致。
- 可观测结果：测试人员最终能从页面、接口、日志、设备状态、云端数据、H5 页面或推送消息中观察到什么结果。

### 2.3 用例设计方法选择

- 场景法：覆盖主流程、备选流程、异常退出和返回恢复。
- 等价类/边界值：覆盖输入框、密码规则、二维码内容、设备数量、重试次数、超时时间。
- 判定表：覆盖“多条件决定一个结果”的规则，例如系统 + 地区 + 语言 + 商店可用性决定跳转渠道。
- 状态迁移：覆盖登录态、设备连接、扫码识别、4G 锁卡、删除设备等状态变化。
- Pairwise/正交：覆盖网络、机型、账号、设备、固件、入口等多维组合，避免全排列。
- 错误推测：补充快速点击、重复提交、缓存残留、接口超时、设备离线、云端不同步、权限越权等高危点。

## 3. 关联规则

测试点文件面向需求评审和测试分析，保持纯思维导图大纲，不使用 `REQ`、`TP` 等编号。测试用例文件可保留用例编号，方便执行和缺陷引用。

- 测试点文件：每个需求功能必须是一个独立一级分支；该功能提取出的每个测试点必须是该一级分支下的独立二级分支；叶子节点只写具体风险和验证关注点。
- 测试用例文件：保留 `用例编号`，并通过 `需求功能`、`关联测试点` 字段对应测试点文件中的分支名称。
- 需求覆盖矩阵：使用需求功能名称、测试点名称、对应用例编号来表达覆盖关系。
- 测试清单：准备项说明用途，并标注关联的需求功能名称，不使用编号。

测试点文件写法：

- `## 新平台设备扫码/手动添加引导`
- `### 新平台识别与旧流程拦截`
- `- 触发条件：扫码识别为新平台设备；验证目标：当前 App 不进入旧配网流程，弹出 xiaovv AI 引导。`

Excel 用例中的映射要求：

- `关联测试点` 字段填写测试点文件中的二级分支名称。
- `需求来源` 字段填写需求文档中的功能名称或原文摘要。
- `需求覆盖矩阵` 必须能从每个需求功能追踪到测试点名称，再追踪到用例编号。
- 若某个测试点暂不能生成用例，覆盖状态标为“待确认”，并在备注说明阻塞原因。

## 4. 测试点文件要求

文件名：`需求名称-测试点.md`

测试点文件是第一份正式交付物。内容必须使用 XMind 可完整导入的 Markdown 标题大纲，定位是“初步需求分析后的测试点脑图”。不要使用 Mermaid、PlantUML、代码块、表格或 HTML，因为 XMind 导入 Markdown 时会把代码块当成普通文本，导致只显示成一个大分支。

测试点提取原则：

- 需求驱动：每个测试点都要能追溯到需求原文、原型图、表格或合理推导风险。
- 科学覆盖：根据需求合理参考场景法、等价类、边界值、状态迁移、异常推测、兼容性矩阵、权限矩阵，但不需要在脑图里写方法名。
- 分析深度：体现资深测试人员对 UI、功能、权限、设备状态、网络、兼容、异常、回归、外部依赖的测试思考。
- 监控摄像机链路完整：关注 App、摄像机设备、云端、H5、系统权限、推送、网络、固件、存储之间的完整链路。
- 内容必须覆盖需求文档“详细需求”下的全部需求功能。
- 测试点必须体现“触发条件 + 验证目标 + 可观察结果”中的至少两项，禁止只输出抽象分类词。
- 每个测试点要尽量写成可验证对象，例如“4G 设备离线且套餐异常同时存在时弹窗按优先级展示”，不要写成“异常测试”。
- 需求功能下必须按风险维度展开，优先考虑主流程、分支规则、输入输出、状态变化、异常恢复、设备状态、网络状态、权限账号、平台地区语言、旧功能回归和外部依赖。
- 输出只能包含 Markdown 标题大纲，不要出现 Mermaid、普通代码块、表格、HTML 或长段落说明。
- 使用 `# 需求名称 测试点` 作为唯一根主题，供 XMind 识别为中心主题。
- 使用 `## 需求功能名称` 表示一级分支，每个一级分支对应一个需求功能、功能模块或主业务链路。
- 使用 `### 测试点方向` 表示二级分支，例如业务规则、异常恢复、输入输出、状态流转、兼容回归、外部依赖、待确认风险。
- 使用 `#### 具体关注点` 表示三级或叶子节点。为保证 XMind 导入稳定，测试点文件优先使用标题层级，不使用 `-` 列表。
- 一级分支至少 4 个，优先按需求功能或主业务链路拆分，避免所有测试点堆在一个 `##` 分支下。
- 每个重要一级分支下至少有 3 个 `###` 二级分支；每个二级分支下至少有 2 个 `####` 具体关注点。
- 每个重要一级分支只输出适用且有验证价值的维度；核心链路通常应包含业务规则、关键异常/恢复、数据一致性、兼容或环境风险，但不要机械凑满维度。
- 对有输入控件、配置项、密码、昵称、设备名、验证码、链接、二维码、H5 参数等内容的需求，必须出现输入域/输出域相关测试点。
- 对有状态变化的需求，必须出现状态事件相关测试点，例如失败、重试、中断、恢复、超时、服务端异常、设备离线。
- 对跨页面、跨 App、H5、应用商店、云端或设备端联动的需求，必须出现用户旅程和跨功能交互测试点。
- 对摄像机 App 核心链路，必须检查 App 操作、云端接口、设备端状态、本地缓存和返回 App 后状态是否一致。
- 对需求中的模糊规则，测试点文件中可以保留“待确认风险”分支，但必须同时在需求批注中指出原文位置、问题、影响和建议。
- 对复杂条件组合，必须出现组合测试相关测试点或高危组合。
- 禁止输出无意义节点，例如“验证功能正常”“检查兼容性”“测试异常情况”这类没有对象、条件和预期的内容。
- 禁止重复节点：同一风险不要同时出现在“业务层”“用户旅程”“回归影响”下重复描述；可放到最贴切的分支。
- 测试点粒度以“能指导后续生成用例”为准，不要细到测试步骤，也不要粗到无法执行。
- 每个 `### 测试点名称` 下至少要有 2 个叶子节点，且叶子节点必须体现触发条件、关注风险或预期观察点之一。
- 同一需求功能下的测试点建议控制在 6 到 12 个；复杂需求可适当增加，但必须合并同类风险。
- 若需求描述不足以设计测试点，测试点文件中只保留“待确认风险”分支，同时在 Word 批注中提出补充建议，不要凭空编造细节。
- 不允许出现 Markdown 表格。
- 不允许输出测试清单、测试用例步骤、Excel 字段说明或长篇段落。
- 思维导图必须有多层级、多分支，不要只有一层功能列表。
- 每个一级分支对应一个需求功能、功能模块或主业务链路。
- 每个一级分支下根据该需求实际情况选择多个分支展开，例如功能流程、异常场景、边界条件、权限/账号、设备状态、网络状态、平台/地区/语言、外部依赖、回归影响、待确认风险。
- 如果一个功能只提取出 1 个测试点，说明分析过粗，必须继续按功能规则、输入输出、状态异常、兼容回归等角度拆成多个二级分支。
- 不要求每个测试点下面都固定出现“测试项”；不要机械给所有节点套同一组子分支。
- 分支粒度以“评审时能看懂该需求需要测哪些方向”为准，后续详细测试项和执行步骤放到测试清单与测试用例中。
- 思维导图节点要体现测试设计思考，不要只照抄需求标题。
## 5. Word 需求批注要求

文件名：`需求名称+需求批注.docx`

这份文件必须是基于原 Word 需求文档生成的批注文档，不是 Markdown 批注清单。

操作要求：

- 不修改、不覆盖原需求文档。
- 复制原 Word 需求文档到输出文件夹。
- 将副本命名为 `需求名称+需求批注.docx`。
- 在副本中定位需要批注的原文、标题、表格单元格、段落或原型图附近说明文字。
- 选中对应文字范围后插入 Word 批注。
- 不要只在文末追加汇总批注。
- 如果批注对象是图片或原型图且无法选中图片内文字，就选中图片前后的标题、说明文字或所在段落插入批注，并说明“针对下方/上方原型图”。

批注内容格式：

```text
[测试视角][P0][功能不明确]
问题：
影响：
建议：
```

```text
[产品视角][P1][可优化建议]
问题：
影响：
建议：
```

批注重点：

- 功能描述不详细或模糊。
- 功能入口、流程、按钮行为、状态流转不明确。
- 异常场景、边界条件、失败兜底缺失。
- 兼容场景考虑不全，例如设备型号、固件、平台、地区、语言、账号类型。
- 权限、分享、会员、云服务、4G/SIM 卡、SD 卡等条件未说明。
- 文案、原型图、文字需求不一致。
- 验收标准不明确。
- 用户体验、风险提示、失败恢复方式可优化。

## 6. 测试清单文件要求

文件名：`需求名称+测试清单.md`

输入依据：需求文档、测试点文件。

作用：列清楚执行本需求测试前必须准备什么、哪些环境/工具/物资会阻塞测试、冒烟和回归需要检查什么，每一个都需要进行详细描述。

测试清单必须覆盖：

- 测试环境与工具。
- 摄像机设备、固件、配件、SIM 卡、SD 卡等物资。
- 账号、权限、会员/云服务状态。
- 网络、地区、语言、系统权限等外部条件。
- 后台开关、灰度配置、H5、云端接口、应用商店等服务依赖。
- 冒烟测试范围。
- 回归测试范围。
- 发布前确认项。
- 待确认问题与阻塞项。

测试清单写法要求：

- 按“必须准备”“按需准备”“阻塞项”“回归关注”分组，避免只罗列物资。
- 每个准备项说明用途和关联需求功能，便于判断缺少该物资会阻塞哪些测试。
- 不要把具体测试步骤写入测试清单；步骤只写在 Excel 用例中。
- 对外部依赖要写清楚负责人或来源，例如后台开关、H5 链接、应用商店配置、接口错误码、测试账号权限。

摄像机 App 常见准备项：

- 摄像机设备：新平台/旧平台、Wi-Fi/4G、电池设备、云台设备、门铃设备、支持/不支持目标能力的设备。
- 设备状态：在线、离线、关机、重启中、升级中、充电中、低电量、恢复出厂。
- 固件：最低支持版本、旧固件、待升级固件。
- 手机：Android/iOS、国内/海外、不同语言/地区、不同系统版本。
- 账号：主账号、分享账号、访客账号、会员/非会员、云存储开通/过期账号。
- 网络：2.4G Wi-Fi、5G Wi-Fi、移动网络、弱网、断网、路由器断网。
- 外部服务：后台开关、灰度配置、H5、云存储、推送、短信/邮箱、应用商店、4G 套餐服务。
- 物理场景：移动物体、人形、声音源、暗光/夜视、门铃按压、SD 卡、SIM 卡。

## 7. 测试用例 Excel 要求

文件名：`需求名称+测试用例.xlsx`

输入依据：需求文档、测试点文件。

作用：对需求和测试点进行覆盖，输出可执行测试用例。

Excel 至少包含 `测试用例` 和 `需求覆盖矩阵` 两类内容。复杂需求可增加测试准备、待确认问题与风险等内容。

测试用例内容必须包含：用例编号、需求功能、关联测试点、需求来源、模块、用例标题、优先级、用例类型、设计方法、前置条件、测试数据/资源、测试步骤、预期结果、适用平台、适用设备/固件、备注。

需求覆盖矩阵必须体现：需求功能、测试点名称、对应用例编号、覆盖状态、备注。覆盖状态使用：已覆盖、部分覆盖、待确认、不适用。

用例编写规则：

- 必须覆盖测试点文件中的 P0/P1 测试点。
- 每条用例都要关联测试点名称。
- 复杂测试点要拆成正常、异常、权限、边界、兼容、回归等多条用例。
- 测试用例必须从测试点文件继续细化，不允许只覆盖正向流程；P0/P1 功能至少按风险选择正向、逆向、异常中断、边界数据、权限/账号、网络环境、兼容回归，不要机械每类都生成一条。
- 涉及输入控件时，必须有等价类、边界值、特殊值和输出验证用例。
- 涉及状态变化时，必须覆盖关键“状态 + 事件”组合，尤其是失败、中断、重试、恢复。
- 涉及跨 App/H5/应用商店/云端/设备端时，必须覆盖完整用户旅程和返回原路径后的状态恢复。
- 涉及多维条件时，用 Pairwise/正交思想选择代表组合，并补充高危组合；组合依据写入备注或测试数据。
- 性能、稳定性和资源类用例应覆盖页面响应、设备连接耗时、内存/CPU、流量、电量、长时间运行或反复进出页面。
- 去重要求：若两条用例只有平台、机型、网络等一个低风险条件不同，且步骤和预期完全相同，应合并为一条兼容/组合用例，在测试数据中列代表组合。
- 有效性要求：每条用例必须包含明确前置条件、可执行步骤和可判定预期；禁止出现“正常显示”“功能正常”“兼容正常”等泛化预期。
- 覆盖矩阵中不得为了提高数量重复关联同一测试点；重复覆盖要说明是不同风险、不同状态或不同组合。
- 预期结果不能只写“成功”或“失败”，要写清楚页面表现、数据状态、设备状态、云端/H5/推送结果。
- 不要把测试准备项当成测试步骤；准备内容写入测试清单或前置条件。
- `设计方法` 字段必须填写场景法、等价类、边界值、判定表、状态迁移、Pairwise/正交、错误推测、兼容矩阵、回归验证等之一。
- 步骤数量建议 3 到 8 步；超过 8 步通常需要拆分或把准备动作前移到前置条件。
- 一个用例最多关联 3 个测试点；超过 3 个通常说明用例目标过散，需要拆分。
- 对组合测试用例，在测试数据/资源中写出代表组合，不要把每个组合拆成多条重复用例。

## 8. 交付前质量门禁

生成文件前必须自检，发现问题要先修正再交付。

测试点文件自检：

- 是否是 XMind 可完整导入的 Markdown 标题大纲，且无 Mermaid、代码块、表格。
- 是否所有一级分支都是需求功能名称，二级、三级节点都是清晰的测试点方向，且没有 `REQ`、`TP` 等编号。
- 是否每个需求功能都有独立一级分支，且围绕对应功能形成多层级标题分支。
- 是否存在“验证功能正常”“测试异常情况”“检查兼容性”等空泛节点；有则改写或删除。
- 是否存在同一风险在多个分支重复出现；有则合并到最合适的分支。
- 是否每个核心需求至少覆盖主流程、关键异常、状态/数据一致性、必要兼容或回归影响。

测试用例自检：

- 每条用例是否有明确前置条件、步骤、预期结果和设计方法。
- 每条用例是否能通过需求功能和关联测试点追溯到测试点文件。
- 是否存在步骤和预期几乎相同的重复用例；有则合并为组合用例。
- 是否存在不可执行或无法判定的用例；有则移动到待确认问题。
- 覆盖矩阵是否存在未覆盖的 P0/P1 测试点。

Word 批注自检：

- 批注是否插入在原文相关位置，而不是文末汇总。
- 批注是否说明问题、影响和建议。
- 批注是否优先覆盖需求模糊、规则缺失、异常不清、兼容缺失、验收标准不明确的问题。

最终回复自检：

- 必须列出输出文件夹、四份文件路径、测试点数量、用例数量、批注数量。
- 若存在待确认阻塞项，必须简要列出最高风险的 3 到 5 条。

## 9. 优先级规则

- P0：主流程、发布阻塞、数据/安全风险、核心摄像机能力，例如登录、添加设备、实时预览、告警、录像、回放、权限控制、删除设备。
- P1：重要分支、常见异常、设备状态变化、网络异常、兼容差异、关键回归。
- P2：低频边界、弱提示、文案展示、体验优化、非核心配置项。

## 10. 最终回复要求

完成后只做简要说明，列出：

- 输出文件夹路径。
- 四份文件路径。
- 测试点数量。
- Word 批注数量。
- Excel 用例数量。
- 仍需产品/研发确认的高风险问题。
